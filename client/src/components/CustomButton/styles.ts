import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: "100%",
    padding: 10,
    borderRadius: 5,
    marginTop: 15,
  },
  label: {
    fontFamily: "Roboto-Bold",
    fontSize: 20,
    textAlign: "center"
  }
})